name: Say hello

on:
  workflow_dispatch:
    inputs:
      my_name:
        description: 'Come ti chiami?'
        required: true

jobs:

  say_hello:
    runs-on: ubuntu-latest
    steps:

      - name: Say hello
        run: echo "hello ${{ github.event.inputs.my_name }}"